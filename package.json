{
  "name": "playtest-studio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A browser game-building studio where Kane CLI is the automated playtester: build a game, declare its rules in plain English, and Kane proves they hold in a real browser. Built in Kiro.",
  "scripts": {
    "dev": "vite --port 5173 --strictPort",
    "build": "tsc && vite build",
    "preview": "vite preview --port 4173 --strictPort",
    "typecheck": "tsc --noEmit",
    "studio": "node scripts/studio.mjs",
    "studio:api": "node scripts/studio-server.mjs",
    "kane:verify": "node scripts/kane-verify.mjs"
  },
  "devDependencies": {
    "typescript": "5.6.3",
    "vite": "5.4.10"
  }
}
